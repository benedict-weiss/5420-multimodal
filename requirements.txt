# Core data dependencies
numpy<2                  # muon transitive dep chain breaks on numpy 2.x
scipy>=1.10              # used in tests (scipy.sparse) and by scanpy/anndata
pandas>=2.0              # used directly in preprocessing.py
anndata>=0.9
# anndata/mudata import path requires zarr at runtime
zarr<3
scanpy>=1.9,<2
muon>=0.1,<1
scikit-learn>=1.3

# Pathway tokenization — pinned due to pkg_resources dependency on setuptools<78.
# gseapy 0.10.x calls DataFrame.append() which was removed in pandas 2.0; attention_graph.py
# restores it as a pd.concat shim at call-time so this combination still works.
# gseapy >=1.0 requires Rust at build-time and no pre-built wheels exist for all
# cluster Python versions, so upgrading is deferred.
gseapy==0.10.8
setuptools<78            # gseapy 0.10.8 needs pkg_resources (removed in setuptools>=78)

# Testing
pytest>=8
pytest-cov>=4

# Visualization and evaluation (needed for evaluate.py and attention_analysis.py,
# not yet implemented — install when working on those modules)
matplotlib>=3.7
seaborn>=0.13
phate>=1.0

# Graph clustering for attention_graph.py (optional — pipeline falls back to
# sklearn spectral clustering when these are not importable)
leidenalg>=0.10
python-igraph>=0.10

# Model training
# Keep a conservative torch range in base requirements; HPC installs should use a
# CUDA-matched wheel index explicitly (example for CUDA 12.8 clusters):
#   pip install --index-url https://download.pytorch.org/whl/cu128 torch torchvision torchaudio
torch>=2.0,<2.12
